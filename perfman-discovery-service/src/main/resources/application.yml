server:
  port: 8761

spring:
  application:
    name: perfman-discovery-service

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false
  dashboard:
    login: admin
    password: admin

management:
  endpoints:
    web:
      exposure:
        include: "*"
